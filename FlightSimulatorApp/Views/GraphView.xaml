<UserControl x:Class="FlightSimulatorApp.Views.GraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
              xmlns:oxy="http://oxyplot.org/wpf"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FlightSimulatorApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListBox HorizontalAlignment="Left" Height="375" VerticalAlignment="Top" Width="225" Margin="50,30,0,0"
                 ItemsSource="{Binding VM_AttributesList}" SelectionChanged="ListBox_SelectionChanged_1">
        </ListBox>
        
            <oxy:Plot Title="{Binding Title}" Margin="430,30,45,225">
                <oxy:Plot.Series>
                    <oxy:LineSeries ItemsSource="{Binding VM_DataPointsList}"/>
                </oxy:Plot.Series>
            </oxy:Plot>
       
    </Grid>
</UserControl>
